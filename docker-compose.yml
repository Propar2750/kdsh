services:
  pipeline:
    build: .
    volumes:
      - .:/app
    working_dir: /app
    command: python -m pytest tests/ -v
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
