You are implementing a Track A pipeline using Pathway.

You MUST:
1) Read CONTEXT.md and PATHWAY_NOTES.md before coding.
2) Never hallucinate Pathway APIs. If unsure, implement stubs + TODOs + minimal wrappers.
3) Work in tiny increments: each task must produce runnable code + a quick test.
4) Prefer simple, debuggable architecture over cleverness.
5) Log intermediate artifacts: claims, retrieved chunks, verdicts, aggregation.

Output only code changes and brief run instructions.
